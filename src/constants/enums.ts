export enum TransactionType {
  INCOME = "Доход",
  EXPENSE = "Расход",
}

export enum TransactionCategory {
  OTHER = "Другое",
}

export enum TransactionDescription {
  REPLENISHMENT_SAVING_ACCOUNT = "Пополнение сберегательного счёта",
  REPLENISHMENT_CURRENT_ACCOUNT = "Пополнение текущего счёта",
}

export enum ErrorEnum {
  STANDARD_ERROR_MESSAGE = "Извините, произошла ошибка!"
}
